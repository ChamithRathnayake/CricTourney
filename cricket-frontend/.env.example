# CricTourney Frontend Environment Variables

# PocketBase Backend Connection URL
# - Development local fallback: http://127.0.0.1:80
# - Production docker/cloud fallback: defaults to the current browser origin (window.location.origin)
# - Set this variable explicitly if your frontend is hosted separately from the PocketBase backend
VITE_PB_URL=http:/65.1.249.21/api
